"github_com/anshulgoel27/krakend-ratelimit/redis": {
    "connection_pools": [
        {
            "name": "redis_rate_limit_pool",
            "address": "{{ env "REDIS_URL" }}",
            "password": "{{ env "REDIS_PASSWORD" }}"
        }
    ]
}